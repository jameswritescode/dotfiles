bind-key r source-file ~/.tmux.conf
bind-key - split-window -v
bind-key \ split-window -h
bind-key -n C-S-Left swap-window -t -1
bind-key -n C-S-Right swap-window -t +1

bind -n S-Right next-window
bind -n S-Left previous-window

setw -g monitor-activity on
setw -g aggressive-resize on
setw -g mode-keys vi

set -ag terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[2 q'
set -sg escape-time 10
set -g default-terminal "screen-256color"
set -g prefix C-a
set -g prefix2 C-b
set -g history-limit 10000
set -g status-interval 1
set -g allow-rename off
set -g mouse on
set -g visual-activity off

set -g status-fg white
set -g status-bg black

set -g status-left-bg black
set -g status-left-fg white
set -g status-left-length 40
set -g status-left "‹#[fg=white,bold]#(whoami)#[fg=white,nobold]› "

set -g status-right-bg black
set -g status-right-fg white
set -g status-right "‹#[fg=white,bold]#(~/dotfiles/tmux/whatpulse.sh)#[fg=white,nobold]› ‹#[fg=white,bold]%H:%M:%S#[fg=white,nobold]› "

set -g window-status-fg white
set -g window-status-bg black

set -g window-status-format "‹#{?window_activity_flag,#[bg=green]#I:#W#[bg=white],#[fg=white]#I:#W}›"
set -g window-status-current-format "‹#[fg=white,bold]#I:#W#[fg=white,nobold]›"

# It looks like there is a long running bug where these values are reversed.
set -g window-status-activity-bg white
set -g window-status-activity-fg black

set -g message-bg black
set -g message-fg green

set -g message-command-bg black
set -g message-command-fg green

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
run '~/.tmux/plugins/tpm/tpm'
